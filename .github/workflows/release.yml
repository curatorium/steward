name: Release

on:
  push: {tags: ['v*']}

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo curl -1fsSLR https://github.com/curatorium/bash-import/releases/latest/download/bash-import -o /usr/local/bin/bash-import;
          sudo chmod +x /usr/local/bin/bash-import;
          sudo curl -1fsSLR https://github.com/curatorium/bash-import/releases/latest/download/bash-test -o /usr/local/bin/bash-test;
          sudo chmod +x /usr/local/bin/bash-test;

      - name: Build release artifacts
        run: bash-import pack steward -o .dist/steward

      - name: Run checks (shellcheck + tests)
        run: |
          docker run --rm \
            -v "$PWD:/workspace" \
            -v /usr/local/bin/bash-import:/usr/local/bin/bash-import:ro \
            -v /usr/local/bin/bash-test:/usr/local/bin/bash-test:ro \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v /tmp:/tmp \
            -e DOCKER_HOST_DIR="$PWD" \
            -w /workspace \
            neurony/php-8.2:ci \
            bash -c '
              apt-get update -qq && apt-get install -qqy shellcheck >/dev/null 2>&1;
              git config --global --add safe.directory /workspace;
              .githooks/pre-commit
            '

      - name: Create or update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Idempotent: if the tag's release already exists (e.g. an earlier
          # run of this workflow partially succeeded and created the tag),
          # just clobber the asset instead of failing the whole workflow.
          # `gh release create` has no --clobber for the create-vs-update
          # distinction, so we branch explicitly.
          if gh release view "${{ github.ref_name }}" >/dev/null 2>&1; then
            gh release upload "${{ github.ref_name }}" .dist/* --clobber
          else
            gh release create "${{ github.ref_name }}" --title "${{ github.ref_name }}" --generate-notes --latest .dist/*
          fi
